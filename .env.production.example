# =================================
# Production Environment
# =================================
# Copy to .env.production and fill in real values.
# Used by docker-compose.prod.yml

NODE_ENV=production

# Database
POSTGRES_USER=hardgraph
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=hardgraph
DATABASE_URL=postgresql://hardgraph:CHANGE_ME_STRONG_PASSWORD@postgres:5432/hardgraph?schema=public

# Auth
JWT_SECRET=CHANGE_ME_RANDOM_SECRET_AT_LEAST_32_CHARS
JWT_EXPIRES_IN=7d

# GitHub OAuth (optional)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_CALLBACK_URL=https://api.hardgraph.io/api/auth/github/callback

# URLs — set to your actual domain
API_PORT=4000
NEXT_PUBLIC_API_URL=https://api.hardgraph.io
NEXT_PUBLIC_APP_URL=https://hardgraph.io
