import { NextResponse } from 'next/server';

export async function GET() {
  // OG image generation — will be implemented later
  return NextResponse.json({ message: 'OG image endpoint placeholder' });
}
